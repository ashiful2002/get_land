import React from "react";
import { Link } from "react-router";

const Logo = () => {
  return (
    <Link to="/" className="btn btn-ghost text-xl">
      Logo
    </Link>
  );
};

export default Logo;
